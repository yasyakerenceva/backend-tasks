<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title %></title>
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
			rel="stylesheet"
			integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
			crossorigin="anonymous"
		/>
	</head>
	<body>
		<% if (created) { %>
		<div class="alert alert-success">Note has been created!</div>
		<% } %>

		<div class="container">
			<h1><%= title %></h1>

			<form action="/" method="post" class="mb-2">
				<div class="form-group mb-2">
					<input
						type="text"
						class="form-control"
						name="title"
						required
					/>
				</div>

				<button class="btn btn-primary" type="submit">Create!</button>
			</form>

			<% if (notes.length) { %>
			<div class="card">
				<ul class="list-group">
					<% for (let note of notes) { %>
					<li class="list-group-item">
						<div
							class="list-item-content d-flex justify-content-between align-items-center"
						>
							<span><%= note.title %></span>
							<div class="d-flex align-items-center">
								<button
									type="button"
									class="btn btn-outline-primary me-3"
									data-type="update"
									data-id="<%= note.id  %>"
								>
									Обновить
								</button>
								<button
									type="button"
									class="btn btn-danger"
									data-type="remove"
									data-id="<%= note.id  %>"
								>
									&times;
								</button>
							</div>
						</div>
						<div
							class="list-item-content d-flex justify-content-between align-items-center d-none"
						>
							<input
								type="text"
								name="note"
								value="<%= note.title %>"
								data-value="<%= note.title %>"
							/>
							<div class="d-flex align-items-center">
								<button
									type="button"
									class="btn btn-success me-3"
									data-type="save"
									data-id="<%= note.id  %>"
								>
									Сохранить
								</button>
								<button
									type="button"
									class="btn btn-danger"
									data-type="cancel"
									data-id="<%= note.id  %>"
								>
									Отменить
								</button>
							</div>
						</div>
					</li>
					<% } %>
				</ul>
			</div>

			<% } else { %>
			<p>No notes!</p>
			<% } %>
		</div>

		<script src="/app.js"></script>
	</body>
</html>
